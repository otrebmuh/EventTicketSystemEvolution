# ============================================
# CONFIGURACIÓN PARA ENVÍO REAL DE EMAILS
# ============================================
#
# Este archivo configura el sistema para enviar emails REALES
# usando Gmail SMTP en lugar de MailHog (servidor de prueba local)
#
# PASOS PARA CONFIGURAR:
# 
# 1. CREAR CONTRASEÑA DE APLICACIÓN DE GMAIL:
#    a. Ve a: https://myaccount.google.com/security
#    b. Habilita "Verificación en dos pasos" (si no está habilitada)
#    c. Busca "Contraseñas de aplicaciones"
#    d. Selecciona "Correo" y "Otro (nombre personalizado)"
#    e. Escribe "Event Booking System"
#    f. Copia la contraseña de 16 caracteres generada
#
# 2. CONFIGURAR ESTE ARCHIVO:
#    a. Copia este archivo: cp .env.gmail.complete .env
#    b. Reemplaza "tu-email@gmail.com" con tu email real
#    c. Reemplaza "xxxx xxxx xxxx xxxx" con la contraseña de aplicación
#
# 3. REINICIAR EL SERVICIO:
#    docker-compose up -d --build auth-service
#
# 4. PROBAR:
#    - Ve a http://localhost:3001/register
#    - Registra un usuario con tu email real
#    - Verifica que recibas el email de verificación
#
# ============================================

# Gmail SMTP Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=xxxx xxxx xxxx xxxx
MAIL_FROM=tu-email@gmail.com

# SMTP Settings (necesarios para Gmail)
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true
MAIL_SMTP_STARTTLS_REQUIRED=true
MAIL_SMTP_SSL_TRUST=smtp.gmail.com
MAIL_DEBUG=true

# Frontend URL
FRONTEND_URL=http://localhost:3001

# ============================================
# ALTERNATIVAS A GMAIL
# ============================================

# SendGrid (100 emails gratis/día)
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=tu-sendgrid-api-key
# MAIL_FROM=noreply@tudominio.com
# MAIL_SMTP_AUTH=true
# MAIL_SMTP_STARTTLS_ENABLE=true
# MAIL_SMTP_STARTTLS_REQUIRED=true
# MAIL_SMTP_SSL_TRUST=smtp.sendgrid.net

# AWS SES
# MAIL_HOST=email-smtp.us-east-1.amazonaws.com
# MAIL_PORT=587
# MAIL_USERNAME=tu-aws-ses-smtp-username
# MAIL_PASSWORD=tu-aws-ses-smtp-password
# MAIL_FROM=noreply@tudominio.com
# MAIL_SMTP_AUTH=true
# MAIL_SMTP_STARTTLS_ENABLE=true
# MAIL_SMTP_STARTTLS_REQUIRED=true
# MAIL_SMTP_SSL_TRUST=email-smtp.us-east-1.amazonaws.com

# ============================================
# VOLVER A MAILHOG (desarrollo local)
# ============================================
# Para volver a usar MailHog, simplemente elimina o renombra
# el archivo .env y reinicia el servicio:
# 
# mv .env .env.backup
# docker-compose up -d --build auth-service
# ============================================

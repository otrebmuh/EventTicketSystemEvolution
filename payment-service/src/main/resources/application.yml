server:
  port: 8080
  # HTTPS configuration (uncomment for production)
  # ssl:
  #   enabled: true
  #   key-store: classpath:keystore.p12
  #   key-store-password: ${SSL_KEY_STORE_PASSWORD}
  #   key-store-type: PKCS12
  #   key-alias: eventbooking
  # Enforce HTTPS in production
  forward-headers-strategy: framework
  # Security settings
  error:
    include-message: never
    include-binding-errors: never
    include-stacktrace: never
    include-exception: false
  # Compression
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 1024

spring:
  application:
    name: payment-service
  config:
    import: optional:classpath:application-monitoring.yml
  
  datasource:
    url: jdbc:postgresql://localhost:5435/payment_service
    username: payment_user
    password: payment_password
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    com.eventbooking: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true

service:
  auth:
    secret: ${SERVICE_AUTH_SECRET:defaultServiceSecret123456789012345678901234567890}

services:
  auth:
    url: ${AUTH_SERVICE_URL:http://localhost:8080}
  event:
    url: ${EVENT_SERVICE_URL:http://localhost:8081}
  ticket:
    url: ${TICKET_SERVICE_URL:http://localhost:8082}
  notification:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}

jwt:
  secret: ${JWT_SECRET:ThisIsAStrongSecretKeyForEventBookingSystem2025!}
  expiration: 86400000

resilience4j:
  circuitbreaker:
    instances:
      authService:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 30s
        failureRateThreshold: 50
      ticketService:
        registerHealthIndicator: true
        slidingWindowSize: 20
        minimumNumberOfCalls: 10
        permittedNumberOfCallsInHalfOpenState: 5
        waitDurationInOpenState: 60s
        failureRateThreshold: 40
      paymentGateway:
        registerHealthIndicator: true
        slidingWindowSize: 15
        minimumNumberOfCalls: 8
        permittedNumberOfCallsInHalfOpenState: 4
        waitDurationInOpenState: 45s
        failureRateThreshold: 30
        slowCallDurationThreshold: 5s
        slowCallRateThreshold: 60
  
  retry:
    instances:
      authService:
        maxAttempts: 3
        waitDuration: 1s
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
      ticketService:
        maxAttempts: 3
        waitDuration: 500ms
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
      paymentGateway:
        maxAttempts: 2
        waitDuration: 2s
        enableExponentialBackoff: false

stripe:
  api-key: ${STRIPE_API_KEY:}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET:}

aws:
  region: ${AWS_REGION:us-east-1}
  access-key-id: ${AWS_ACCESS_KEY_ID:}
  secret-access-key: ${AWS_SECRET_ACCESS_KEY:}
  sqs:
    # Use AWS_ENDPOINT_OVERRIDE for local (LocalStack), leave empty for production (real AWS)
    endpoint: ${AWS_ENDPOINT_OVERRIDE:}
    payment-events-queue: ${AWS_SQS_PAYMENT_EVENTS_QUEUE:payment-events-queue}
    payment-events-dlq: ${AWS_SQS_PAYMENT_EVENTS_DLQ:payment-events-dlq}
  sns:
    # Use AWS_ENDPOINT_OVERRIDE for local (LocalStack), leave empty for production (real AWS)
    endpoint: ${AWS_ENDPOINT_OVERRIDE:}
    payment-events-topic: ${AWS_SNS_PAYMENT_EVENTS_TOPIC:arn:aws:sns:us-east-1:000000000000:payment-events}

---
spring:
  config:
    activate:
      on-profile: docker
  
  datasource:
    url: jdbc:postgresql://payment-db:5432/payment_service

services:
  auth:
    url: http://auth-service:8080
  event:
    url: http://event-service:8081
  ticket:
    url: http://ticket-service:8082
  notification:
    url: http://notification-service:8084

jwt:
  secret: ${JWT_SECRET:ThisIsAStrongSecretKeyForEventBookingSystem2025!}
  expiration: 86400000